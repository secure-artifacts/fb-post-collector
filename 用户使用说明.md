# Facebook 贴文数据采集工具使用说明

这是一款本地运行的软件，用来把 Facebook 贴文或专页内容采集出来，并写入你的 Google 表格。

软件会在你的电脑上打开一个管理页面。你只需要在页面里创建项目、登录账号、选择表格，然后点击运行。

## 使用前需要准备

1. Windows 电脑
2. Google Chrome 浏览器
3. 一个可以正常登录的 Facebook 账号
4. 一个 Google 账号，用来授权写入 Google 表格
5. 一个 Gyazo 账号或软件内置的 Gyazo 上传权限
6. 如果需要识别视频里的声音文字，需要安装 Whisper

软件已经内置：

- ffmpeg


- yt-dlp

一般用户不需要单独安装它们。

图片文字识别需要 Tesseract。

如果你拿到的软件已经内置 Tesseract，就不需要单独安装。

如果“环境检测”页面显示 Tesseract 不可用，图片 OCR 文字就不能识别，需要先安装 Tesseract，或者联系软件提供方获取内置 Tesseract 的版本。

Whisper 不内置。如果你不需要识别视频声音，可以先不安装 Whisper。

打开左侧“环境检测”页面，可以看到当前软件版本，以及 Tesseract、语言包、ffmpeg、Whisper、yt-dlp 是否可用。如果有几项缺失，点击“一键安装缺失组件”，软件会自动把缺的组件补齐。安装过程中请不要关闭软件。

## 启动软件

双击软件启动。

启动后会自动打开一个管理页面，同时会在任务栏右下角显示托盘图标。

关闭网页不会退出软件。以后要点任务栏托盘图标，选择“打开管理页面”。

要完全退出时，请右键托盘图标，选择“退出软件”。

更新安装时，安装程序会先关闭正在运行的软件，避免因为进程占用而装不上。

## 第一次使用

### 1. 登录 Google

打开左侧菜单里的“授权设置”。

点击“登录 Google 账号”。

浏览器会弹出 Google 登录页面，请选择你要写入表格的 Google 账号并完成授权。

如果以后想换 Google 账号，可以点击“删除 Google 授权”，然后重新登录。

### 2. 创建浏览器账号

打开左侧菜单里的“浏览器账号”。

点击新增浏览器账号。

建议一个 Facebook 账号创建一个浏览器账号。

创建完成后，点击“打开登录”。

软件会打开一个专门用于采集的 Chrome 浏览器窗口。请在这个窗口里登录 Facebook。

登录完成后，可以关闭这个 Chrome 窗口。

以后运行项目时，软件会自动使用这个已登录的账号。

这个浏览器账号不会影响你平时正在使用的 Chrome。

## 创建项目

打开左侧菜单里的“项目”。

创建项目时需要选择项目类型：

- 贴文项目
- 专页项目

项目类型创建后不能修改。如果选错了，请删除项目后重新创建。

## 贴文项目

贴文项目适合这种情况：

你的 Google 表格里已经有一列 Facebook 贴文链接，软件按行读取这些链接，然后把采集结果写回同一张表。

需要填写：

- Google 表格链接
- 分表名称
- 贴文链接在哪一列
- 数据从第几行开始
- 采集结果从哪一列开始写入
- 抓取日志列，例如 AK。成功抓取后会写入“已抓取”记录
- 使用哪个浏览器账号。可以同时勾选多个账号，软件会按顺序轮询抓取

然后在“字段配置”里勾选你要导出的字段，并拖动调整顺序。每个启用字段后面会显示写入列号。默认从“写入开始列”连号，例如开始列是 D，后面就是 E、F、G。如果以后要换列，直接改字段后面的列号即可。

字段配置可以导出成 JSON，也可以再导入到其他项目。

保存项目后，点击“手动运行”即可开始采集。

下次再运行时，软件会按这个顺序判断每一行：

1. 抓取日志列已经有记录：直接跳过
2. 日志列是空的，但写入开始列已经有数据：也跳过
3. 两列都是空的：开始抓取这条新增链接

所以你只要继续往链接列追加新贴文，写入列留空，软件就只会跑新增的行。如果选择“覆盖重抓”，则不会跳过。

## 专页项目

专页项目适合这种情况：

你想输入 Facebook 专页链接，让软件自动采集某个时间范围内这个专页发布过的贴文。

专页项目需要两个分表：

1. 专页链接分表
2. 数据写入分表

专页链接分表固定格式：

- A 列：专页链接
- B 列：完成情况
- C 列：续跑信息

你只需要把要采集的专页链接放在 A 列，从第 2 行开始放。

项目里还需要选择：

- 开始日期时间
- 结束日期时间
- 数据写入分表名称
- 使用哪个浏览器账号

运行后，软件会把采集到的贴文一行一行追加到数据写入分表。

## 可以导出的内容

根据项目配置，软件可以导出：

- 贴文链接
- 作者 ID
- 作者名字
- 作者头像
- 作者主页链接
- 贴文图片或视频封面
- 图片链接
- 图片 OCR 文字
- 图片 OCR 中文翻译
- 视频音频文字
- 视频音频中文翻译
- 贴文正文
- 贴文正文中文翻译
- 贴文发布时间
- 点赞量
- 评论量
- 分享量
- 贴文 ID
- 贴文类型
- 视频播放量
- 原创或转贴
- 抓取状态
- 错误提示
- 抓取时间

如果表格里想直接显示图片，请勾选“贴文多媒体”。  
如果只想要图片链接，请勾选“贴文多媒体链接”。

## 贴文类型说明

软件会尽量判断贴文类型：

- 图文贴：带图片的贴文
- 短视频：Reels 或视频贴文
- 彩贴：只有文字和背景的贴文
- 合成图：上方像彩贴，下方还有图片或分享内容的贴文

Facebook 页面样式经常变化，个别贴文类型可能会判断不准。

## 识别语言设置

OCR 语言用于识别图片里的文字。

使用 OCR 前，请先到“环境检测”页面确认 Tesseract 可用，并且对应语言包可用。

Whisper 语言用于识别视频里的声音。

如果不确定视频是什么语言，可以让 Whisper 自动识别。

如果你明确知道视频语言，比如葡萄牙语，可以在项目里选择对应语言，这样速度更快，结果也更稳定。

## 手动运行和停止

在项目列表里点击“手动运行”。

运行开始后，可以在“运行记录”里查看状态。

如果任务还没完成，可以点击“停止”。

停止不会强行中断当前正在处理的贴文。软件会等当前贴文处理完成后再停止。

专页项目停止后，会把续跑信息写入专页链接分表的 C 列。下次再次运行时，会尽量从上次停止的位置继续。

## 运行记录

打开左侧菜单里的“运行记录”。

你可以看到每次运行的状态：

- 运行中
- 正在停止
- 已停止
- 成功
- 有错误
- 失败

点击运行记录 ID，可以查看每一行的处理情况。

如果某一条链接失败，错误列会写比较容易理解的提示，例如：

- 链接无效或无权限查看
- 需要登录 Facebook
- 浏览器配置被占用
- 图片上传失败
- 写入表格失败

## Facebook 接口请求限制

为了降低账号被限制的风险，软件会控制请求速度。

每个 Facebook 账号每天有请求次数限制。

如果某个账号当天达到上限，页面会显示提示。你可以等待第二天再继续，或者换另一个浏览器账号运行。

## 关于 Gyazo 图片上传

软件会把采集到的图片或视频封面上传到 Gyazo，然后把稳定的图片链接写入表格。

如果表格需要直接显示图片，软件会写入 Google 表格可识别的图片公式。

如果上传失败，请检查 Gyazo 授权或稍后重试。

## 关于 Whisper

Whisper 用来识别视频里的说话内容。

如果没有安装 Whisper：

- 图片和基础贴文数据仍然可以采集
- 视频音频文字不会生成

如果你需要视频音频识别，请先安装 Whisper，然后在软件的“环境检测”页面确认 Whisper 可用。

```powershell
pip install -U openai-whisper
```

如果你电脑有独立显卡可以使用，可以还安装  torch 来使用 GPU 加速视频音频的导出

```powershell
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```
## 常见问题

### 软件打开后能不能关闭黑色窗口？

不能。

黑色窗口是软件本身。关闭后，管理页面和采集任务都会停止。

### 可以一边采集，一边使用自己的 Chrome 吗？

可以。

软件使用独立的浏览器账号，不会影响你平时使用的 Chrome。

### 可以登录多个 Facebook 账号吗？

可以。

在“浏览器账号”页面为每个 Facebook 账号创建一个独立账号，并分别登录。

创建项目时可以同时勾选多个浏览器账号。软件抓取贴文时会按勾选顺序轮询账号，降低单个账号连续请求的风险。如果某个账号当天接口次数已经用完，会优先使用还有额度的账号。

### 为什么有些链接显示无效？

常见原因：

- 贴文被删除
- 贴文不是公开内容
- 当前 Facebook 账号没有权限查看
- 账号需要重新登录
- Facebook 页面临时加载失败

你可以先用对应的浏览器账号打开这条链接，确认是否能正常查看。

### 为什么视频没有音频文字？

可能原因：

- 没有安装 Whisper
- 视频本身没有清晰说话声音
- 视频下载失败
- Facebook 临时限制访问
- 选择的语言不正确

可以在项目里调整 Whisper 语言后重新运行。

### 为什么 Google 表格没有写入？

请检查：

- 是否已经登录 Google
- Google 账号是否有这个表格的编辑权限
- 表格链接是否正确
- 分表名称是否填写正确
- 写入列是否被保护

### 为什么图片不显示？

请检查：

- Gyazo 上传是否成功
- 表格是否允许加载外部图片
- 图片链接是否可以在浏览器中打开

## 推荐使用方式

1. 先用少量链接测试
2. 确认字段顺序和表格写入位置正确
3. 再批量运行
4. 每个 Facebook 账号不要短时间采集太多
5. 不同 Facebook 账号建议使用不同浏览器账号
6. 定期查看运行记录，确认是否有失败链接

## 退出软件

关闭控制台窗口即可退出软件。

退出后：

- 管理页面不能继续使用
- 正在运行的任务会停止
- 定时任务也会停止

下次双击软件启动后，可以继续使用。
